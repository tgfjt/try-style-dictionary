const { THEME_NAME } = require('../../constants/theme');
const { white, gray, red } = require('../../constants/color');

module.exports = {
  [THEME_NAME]: {
    color: {
      white,
      gray,
      red
    }
  }
}
